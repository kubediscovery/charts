apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "job-database.fullname" . }}-test-connection"
  labels:
    {{- include "job-database.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "job-database.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
